import "./index.css"

const IconNav = ({ classNames }) => {
  const [navName] = classNames

  return (
    <nav className={classNames} >
      <p>-Icon-</p>
      <p>-Icon-</p>
      <p>-Icon-</p>
    </nav>
  )
}

export default IconNav