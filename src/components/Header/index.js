import './index.css'
import Logo from '../Logo'
import Nav from '../Nav'

const Header = () => {
  return (
    <header className="navbar">
      <Logo />
      <Nav />
    </header>
  )
}

export default Header