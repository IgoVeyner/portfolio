import './index.css'

const ThemeButton = ({ toggleTheme }) => {
  return (
    <button 
      className="theme-button"
      onClick={null}
      >
      -theme-
    </button>
  )
}

export default ThemeButton